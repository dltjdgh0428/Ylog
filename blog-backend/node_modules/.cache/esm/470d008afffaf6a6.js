let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_fff‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_fff‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_fff‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_fff‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_fff‍.w("./api",[["default",["api"],function(v){api=v}]]);_fff‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







// eslint-disable-next-line no-undef
const {PORT, MONGO_URI} = process.env;

mongoose
.connect(MONGO_URI)
    .then(()=>{
        console.log('Connected to mongoDB');
    })
    .catch(e =>{
        _fff‍.g.console.error(e);
});
const router= new Router();
const app = new Koa();

router.use('/api',api.routes())

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());
//포트가 지정되어있지않다면 4천을 사용한다.
const port = PORT||4000;
app.listen(4000, ()=>{
    _fff‍.g.console.log('Listening to port %d',port);
});

